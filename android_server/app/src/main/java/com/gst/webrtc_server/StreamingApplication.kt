package com.gst.webrtc_server

import android.app.Application;
import android.util.Log
import org.freedesktop.gstreamer.GStreamer

class StreamingApplication : Application() {
    override fun onCreate() {
        super.onCreate()
//        Log.i("GstWebrtcServer", "StreamingApplication: In onCreate")
//
//        System.loadLibrary("gstreamer_android")
//        Log.i("GstWebrtcServer", "StreamingApplication: loaded gstreamer_android")
//
//        Log.i("GstWebrtcServer", "StreamingApplication: Calling GStreamer.init")
//        GStreamer.init(this)
//        Log.i("GstWebrtcServer", "StreamingApplication: Done with GStreamer.init")
    }
}
